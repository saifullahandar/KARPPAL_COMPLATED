# KARPPAL

Django REST backend (`backend/`) and React + Vite frontend (`frontend/`).

## Local backend

```bash
cd backend
source venv/bin/activate
python manage.py runserver
```

## Local frontend

```bash
cd frontend
npm install
npm run dev
```
